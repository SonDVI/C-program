
#include <iostream>
int main() {
    const double PI = 3.14159;
    //When you write const - it means the value cannot be changed easily
    // so when some variable is unchanged we use const to protect it
    std::cout << PI;
    return 0;
}