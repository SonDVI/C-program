//CIN

#include <iostream>

using namespace std;

int main() {
    int a;
    cout << "Nhap vao mot so nguyen: ";
    cin >> a; // cin: console input, dùng để nhận dữ liệu từ bàn phím. Nó sẽ chờ người dùng nhập dữ liệu và nhấn Enter trước khi tiếp tục thực hiện chương trình.

    cout << "So nguyen ban vua nhap la: " << a << endl;

    return 0;
}