//LOGICAL OPERATOR
#include <iostream>

using namespace std;

int main() {
    // && is and operator: trả về true nếu cả hai toán hạng đều true, ngược lại trả về false. EX: true && true = true, true && false = false, false && false = false.

    // || is or operator: trả về true nếu ít nhất một trong hai toán hạng là true, ngược lại trả về false. EX: true || true = true, true || false = true, false || false = false.

    // một số nguyên trong cổng and là true, còn số 0 là false.
    
}